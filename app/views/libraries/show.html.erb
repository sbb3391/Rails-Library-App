<% if !@library.books.empty? %>
    <h1>My Books:</h1>
    <%= render partial: "libraries/library_books", collection: @library.books, as: :book %>
<% else %>
  <h1>You have no media in your library.</h1>
<% end %>